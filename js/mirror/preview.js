/**
 * Created by ksl on 2017/6/27.
 */
/*var preview = 0;
var total = {
    init: function() {
        this.order();
        this.play();
        this.user();
        this.relise();
        this.mess();
        $('#btn-preview').click(function() {
            if (preview == 0) {
                preview = 1;
                $('#btn-preview').html('真实模式');
                //console.log(preview+'参观模式');
            } else {
                preview = 0;
                $('#btn-preview').html('参观模式');
                //console.log(preview+'真实模式');
            }
        })
    },
    order: function() {
        order.get(preview);
    },
    play: function() {
        play.get(preview);
    },
    user: function() {
        user.get(preview);
    },
    relise: function() {
        relise.get(preview);
    },
    mess: function() {
        mess.get(preview);
    }
}
$(document).ready(function() {
    total.init();
    setInterval(total.user, 8000);
    setInterval(total.play, 8000);
    setInterval(total.relise, 10000);
    setInterval(total.order, 12000);
    setInterval(total.mess, 16000);
    window.onresize = function() {
        total.order;
        total.play;
        total.user;
        total.relise;
        //mess.get();
    };
});*/